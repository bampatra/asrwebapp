using System;
using System.Linq;
using System.Security.Claims;
using System.Security.Principal;

namespace ASRWebApp.Extensions
{
    public static class ASRExtension
    {
        // Extension method for getting StudentID
        //public static string GetStudentID(this ClaimsPrincipal user)
        //{
        //    if (user.Identity.IsAuthenticated)
        //    {
        //        return user.Claims.FirstOrDefault(v => v.Type == ClaimTypes.).Value;
        //    }

        //    return "";
        //}
    }
}
